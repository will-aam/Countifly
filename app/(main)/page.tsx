// app/(main)/page.tsx
export default function MainIndexPage() {
  // O layout já sabe que, quando o pathname === "/",
  // ele deve renderizar as Tabs de inventário.
  // Então aqui não precisamos renderizar nada específico.
  return null;
}
