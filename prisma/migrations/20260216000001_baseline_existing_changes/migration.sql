-- Baseline: Reconhecer mudanças já existentes no banco

-- Enum SessaoModo (já existe)
-- Coluna `modo` em `sessoes` (já existe)
-- Coluna `usuario_id` em `participantes` (já existe)
-- Colunas em `produtos` (já existem)

-- Esta migration está vazia porque as mudanças já foram aplicadas manualmente
SELECT 1;